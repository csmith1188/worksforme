<%- include('../partials/header.ejs', {pageTitle: 'temp test page' }); %>

    <div class="mainContainer">
        <div class="content">

            <center>

                <h1>HOME</h1>

                <% if(isLoggedIn) { %>
                    <h2>Hello, <%= user.fb_name %>
                    </h2>
                    <% } %>

            </center>

        </div>
    </div>

    <%- include('../partials/footer.ejs') %>